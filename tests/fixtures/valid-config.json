{
  "name": "test-project",
  "version": "1.0.0",
  "description": "Test configuration for unit tests",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "build": "tsup",
    "lint": "eslint src/"
  },
  "dependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "tsup": "^8.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
